'use client';
import {BarChart,Bar,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer} from 'recharts';
import {chartData} from '@/lib/data';
export const colors={Medical:'#00cc91',Security:'#087dff',Fire:'#ff7b09',General:'#8193b4'};
export default function Statistics({period='Today',large=false}:{period?:string;large?:boolean}){const multiplier=period==='This Week'||period==='7 Days'?5:period==='This Month'||period==='30 Days'?18:1;return <div className={`statistics ${large?'large-chart':''}`}><ResponsiveContainer width="100%" height="100%"><BarChart data={chartData.map(d=>({...d,Medical:d.Medical*multiplier,Security:d.Security*multiplier,Fire:d.Fire*multiplier,General:d.General*multiplier}))} margin={{left:-28,right:0,top:12,bottom:0}} barSize={large?28:14}><CartesianGrid stroke="#263241" vertical/><XAxis dataKey="time" tick={{fill:'#d5deea',fontSize:12}} tickLine={false} axisLine={{stroke:'#304052'}}/><YAxis tick={{fill:'#b5c1d2',fontSize:12}} tickLine={false} axisLine={false} domain={[0,multiplier*15]} ticks={[0,5,10,15].map(n=>n*multiplier)}/><Tooltip contentStyle={{background:'#121a25',border:'1px solid #354459',borderRadius:8,color:'#fff'}} cursor={{fill:'#ffffff08'}}/>{Object.entries(colors).map(([key,color])=><Bar key={key} dataKey={key} stackId="incidents" fill={color}/>)}</BarChart></ResponsiveContainer></div>}
